# 08 · P1 设计路线图（人物 / 场景 / 多人）

> 角色：游戏设计负责人 · 日期：2026-08-29
> 输入：01–07 号文档 + `web/index.html` 实现现状
> 这份文档只做三件事：**指出当前设计里已经坏掉的地方、给出修法、排定顺序。**

---

## 00. 先说结论

1. **人物**：8 名干员的被动已全部落地（此前 7 名是空壳），解锁条件也已从"纯次数"改回**成就式混合制**（P1-a 完成）。关键不在解锁本身，而在**三层提示**把"这个游戏可以不用打"这句教学真正送进玩家脑子里。
2. **场景**：M01 的地形是硬编码的 `zone()` 分支，加第二张图必须复制整个文件。P1 要先做**地图配置化**，再谈 M02。
3. **多人**：**不做实时多人**。但我们有一条别家没有的资产——**潮汐是完全确定的**，可以做出零后端的「幽灵竞速」，这在数学上比任何实时多人更公平。

---

## 01. P0 收口（本轮已完成）

| 问题 | 性质 | 处理 |
|---|---|---|
| 出生点 `(-27,0)` 正是撤离点「船坞闸门」，站定 8 秒空手撤离成功 | **通关漏洞** | 出生点外移到闸门外侧滩涂 `(-24,2)`（相距 3.6m，落在 2.5m 判定圈外）；且撤离要求至少携带 1 件战利品 |
| 拾荒者 DPS ≈ 82，玩家 2 秒阵亡 | 数值失控 | 建立 `Combat` 数值总表，单敌 @10m 降至 ≈16 DPS；三敌围攻 3.2 秒致命 |
| 8 名干员中 7 名被动为空（只有 `wading:1`） | **空头支票** | 8 个被动全部落地，每个都有可测量差异 |
| 「干足」的 `wading` 错误作用于背包重量惩罚，而非涉水减速 | 实现与设计不符 | 修正为削减"水造成的减速幅度" |
| 静默期结束瞬间敌人立刻开火 | 手感问题 | 苏醒后给每个敌人 0.6~1.6s 随机反应延迟 |
| §3.1「深水降级为手枪」只有文档承诺、代码没有 | 文档与实现不符 | 实现深水降级；掠波（水面张力）免疫，成为其独占价值 |
| `index.offline.html` 内联着 three.js 却用旧版 UI | 陷阱文件 | 归档至 `game/legacy/`，`web/` 只剩一张脸 |

回归测试：160 项全过（grace 13 / salvage 33 / ops 22 / breach 66 / surge 19 / fp3d 7）。

---

## 02. 人物线

### 2.1 已落地（可验证，不是承诺）

`SALVAGE_OPS` 每个字段驱动一处玩法，且 `test_ops_headless.js` 逐条断言：

| 干员 | 被动 | 实测效果 |
|---|---|---|
| 隼 | 干足 | 涉水减速减半（0.85 → 0.925 速度系数） |
| 沸釜 | 超压 | 霰弹 18 → 24 |
| 渔火 | 潮感 | 底部方位雷达，标记 15m 内敌人 |
| 堤 | 锚定 | 静止 1.5s 后 -25% 伤害（40 → 30） |
| 掠波 | 水面张力 | 深水不降级手枪 + 游泳减速再减半 |
| 渠 | 绳忆 | 移速 +12%（×1.120）、换弹 1.60s → 0.96s |
| 账房 | 打捞权 | ⌾500 → ⌾600 |
| 浚 | 回潮 | 残血 5.20 → 6.24 移速 |

### 2.2 【关键问题】解锁条件丢了教学功能

**设定集 §2.2 写的解锁条件是成就式的**：

| 干员 | 设定集的条件 | 它在教玩家什么 |
|---|---|---|
| 沸釜 | 完成 3 次成功撤离 | 基础流程 |
| 渔火 | 单次带出 ≥ 5,000 | 值得为高价值深入 |
| 堤 | 单局承受 800 伤害并撤离 | 挨打不一定输 |
| **掠波** | **全程不击杀任何敌人**完成撤离 | **这个游戏可以不用打** |
| 渠 | 在满潮阶段成功撤离 | 满潮不是死期 |
| 账房 | 累计带出 ≥ 50,000 | 长期经营 |
| 浚 | 血量 ≤ 10% 时成功撤离 | 残血也能翻盘 |

**现在代码里是 `unlock: 0/2/4/6/8/10/12/14`——纯次数。**

这不只是"简化了"，是**把一个教学系统降级成了登录奖励**。设定集 §2.2 下面那句注释写得很清楚：

> **SKIMMER 的解锁条件（全程不击杀）是刻意的**：它教会玩家"这个游戏可以不用打"，这是撤离游戏最反直觉也最迷人的一课。

现在玩家撤离 8 次解锁掠波，但他从来不知道"可以不开枪"——因为没人用成就逼他去试。**我们失去的是让玩家自己发现核心乐趣的钩子。**

### 2.3 P1 改造：成就式解锁（混合制）

纯成就式有个风险：玩家可能卡在某个条件上解锁不了，产生挫败。所以用**混合制**——每个干员两条路，满足任一即解锁：

| 干员 | 成就路径（主） | 保底路径（次） | 提示文案（锁定卡上显示） |
|---|---|---|---|
| 沸釜 | 单局击杀 5 个敌人并撤离 | 撤离 3 次 | 「用霰弹清出一条路」 |
| 渔火 | 单次带出 ≥ 5,000 | 撤离 5 次 | 「贪一次大的」 |
| 堤 | 单局承受 800 伤害并撤离 | 撤离 7 次 | 「站着打完一场」 |
| 掠波 | **全程 0 击杀撤离** | 撤离 9 次 | **「一枪不开地回来」** |
| 渠 | 满潮阶段成功撤离 | 撤离 11 次 | 「等到水漫过膝盖再走」 |
| 账房 | 累计带出 ≥ 50,000 | 撤离 13 次 | 「攒够五万」 |
| 浚 | 血量 ≤ 10% 时成功撤离 | 撤离 15 次 | 「活着回来，哪怕只剩一口气」 |

**锁定卡必须显示成就路径的进度和提示文案**——锁定卡是钩子，不是障碍。玩家看到"一枪不开地回来"，会想"还能这样玩？"，这就是我们要的反应。

> **✅ P1-a 已于 2026-08-30 落地**（36 项断言，见 `test_unlock_headless.js`）

#### 2.3.1 三层提示：只有锁定卡是不够的

| 层 | 时机 | 作用 | 玩家的心理活动 |
|:--:|---|---|---|
| **锁定卡** | 局前 | 知道 | "原来还能一枪不开地回来？" |
| **局内 HUD** | 局中 | **做得到** | "我现在还是零击杀…这个拾荒者，绕过去还是开枪？" |
| **结算屏** | 局后 | 被奖励 / 被勾住 | "就差 700 就能解锁渔火，再来一局" |

**中间那层是整个设计的关键。** 只在锁定卡上写"一枪不开地回来"，玩家进局三秒就忘了——他遇到第一个拾荒者时会本能开枪，然后彻底失去这个目标。

所以局内 HUD 实时显示「◈ 无痕 · 零击杀 —— 可解锁「掠波」」。这不是作弊，是**把隐形目标变成可见的赌注**。这条对「无痕」尤其关键：其他成就（带出 5000 / 承受 800 伤害）是累积型，玩家随时看得到进度；**只有零击杀是二元的、一次失误就永久破功**。破功不可逆的目标，必须实时可见。

第三层除了解锁祝贺，还要给**「就差一点」**：`再贪 ⌾700 就能解锁「渔火」`。具体差额比抽象鼓励更能驱动下一局。

#### 2.3.2 保底路径延后一档

原次数解锁是 2/4/6/8/10/12/14 次。加成就路径后，保底改为 **3/5/7/9/11/13/15**——刻意比原值慢一档。

原因：如果保底和成就路径一样快，玩家按部就班撤离就解锁了，成就路径形同虚设，教学功能再次落空。**成就路径必须明显更快**（零击杀撤离 1 次即解锁掠波，保底却要 9 次），玩家才有动力去试。

> **实现注记**：`OPS[].unlock` 与 `UNLOCKS[id].runs` 是同一数值的两处引用，
> 已由测试断言两者必须一致。两套数值并存必然产生 bug——测试第一次跑就抓到了不一致。

#### 2.3.3 锁定卡不要用 opacity 压暗

原样式 `.opcard.lock{opacity:.45}` 会把整张卡压得看不清。但锁定卡现在是钩子，上面的字必须读得到。
改为**虚线边框 + 姓名淡化**：一眼能分辨锁定状态，同时成就条件完整可读。

### 2.4 专长系统（P1 后半）

设定集 §2.4 已给框架，实现顺序：

```
干员等级 1  → 被动（已完成）
干员等级 3  → 专长槽 A（两选一）
干员等级 6  → 专长槽 B
干员等级 10 → 专长槽 C + 专属皮肤线
```

**红线**：所有专长必须与潮汐相关（设定集原话："玩家的成长曲线，就是越来越会和水相处"）。
`Combat` 表已证明集中管理数值的价值，专长同样需要一张表，禁止散落。

---

## 03. 场景线

### 3.1 现状：M01 的地形是硬编码的

```js
zone(x,z){
  if(x>=8&&x<=26&&z>=6&&z<=18) return 4.0;
  if(x>=-26&&x<=-8&&z>=-18&&z<=-8) return 3.9;
  ...7 个 if 分支...
}
```

这带来三个后果：
1. 加 M02 必须复制整个 index.html（760KB 的双胞胎）
2. 地形不能数据驱动，无法做地图编辑器 / MOD
3. `nearestHigh()` 等算法与具体地形耦合，改地形要重新验证 AI

### 3.2 P1 第一步：地图配置化（先做这个，再做 M02）

把地形抽象成配置对象：

```js
const MAPS = {
  M01: {
    id:'M01', name:'闸门区', city:'鹿特丹',
    W:60, D:40, CELL:2,
    zones:[
      {rect:[8,6,26,18],   elev:4.0, tag:'高点'},
      {rect:[-26,-18,-8,-8],elev:3.9, tag:'高点'},
      ...
    ],
    baseElev: 2.2,
    spawns:{player:{x:-24,z:2}, scav:12, drone:4},
    extracts:[
      {id:'船坞闸门', pos:{x:-27,z:0}, elev:2.2, closeAt:300},
      ...
    ],
    lootHotspots:[ ... ]   // 高价值区，供情报板显示
  }
};
```

`MapHeight.zone()` 改为查表，`Mission.start()` 读 `MAPS[current]`。
**验收标准**：加一张新图只写一个配置对象，不碰任何逻辑代码。

### 3.3 M02 白象街（重庆）——泄洪机制

设定集原文："**水从顶部倾泻**（上层水库开闸），切断垂直通路"。

M01 教会玩家"水从下往上涨，往高处跑"。M02 要**颠覆这个直觉**——水从上面来。

**地形**：垂直双层。下层街道 0–2m，上层平台/天桥 8–12m，靠 4 处楼梯 + 2 处货运升降机连接。

**独有机制「泄洪」**：
- 在 240s / 420s 两个时刻，上层水库开闸
- 水幕**自上而下**倾泻，覆盖 2 处楼梯（每次随机选 2 处，开局水位表可查——遵守"潮汐可预测"红线）
- 被水幕覆盖的楼梯：通行速度降至 30%，且每秒造成 8 点伤害（冲不冲得过去，是个赌注）
- 视觉：垂直水墙 + 巨大轰鸣（听觉预警 15 秒前开始）

**与核心设计的张力**：满潮时所有人（玩家 + 拾荒者）都挤在上层，而泄洪会在最拥挤的时刻切断上层内部的连接。高地不再安全——这是 §01「敌人也在抢高地」的进阶版。

**实现风险与降级方案**：
当前 `MapHeight` 是单层高度场，真·3D 双层需要占据信息改造。
降级做法（推荐先做）：高度场表达双层（陡崖用 elev 突变），水幕=临时封锁标记，视觉用垂直平面粒子。架构改动可控。

---

## 04. 多人方案

### 4.1 先说实话：实时多人我们做不起

| 项 | 成本 |
|---|---|
| WebSocket / 专用服务器 + 房间管理 | 后端 1 人月 |
| 状态同步（位置、射击命中判定、延迟补偿） | 1–2 人月 |
| 反作弊（当前是纯前端，改数值只需改 localStorage） | 1–2 人月 |
| 静态托管失效 → 需运维 + 服务器费用 | 持续性成本 |

设定集 §10 红线第 5 条明确写了"不做多人、不做 PvP"。README 也承认"真多人实时联机需要独立后端，静态托管给不了"。

**结论：P1 不做实时多人。但这不等于"不能一起玩"。**

### 4.2 我们的独家资产：潮汐是完全确定的

设定集 §10 红线第 1 条：

> 潮汐永远**可预测、不随机** —— 不确定性来自敌人与贪心，不来自机制

这条红线本来是为了保证公平，但它意外给了我们一件别家没有的东西：

**两个玩家在同一时刻，面对的水位曲线在数学上完全一致。**

对比一下：
- Tarkov 的撤离点、战地毒圈、PUBG 缩圈——**都有随机性**，两份录像不是同一个世界
- 我们的潮线——**同一条曲线**，第 300 秒水位必然是 0.90m，无论谁在玩

**这意味着我们可以做「幽灵竞速」，而且比实时多人更公平。** 两个人跑的是同一个物理世界，唯一的变量是人。

### 4.3 方案：幽灵竞速（L0 · 零后端 · 静态托管可用）

**玩家体验**：
1. 你打完一局，结算屏显示「生成潮痕」按钮
2. 得到一段分享码（约 2–3KB 文本）
3. 发给朋友；朋友在部署屏粘贴/打开链接加载你的幽灵
4. 开局后，同场出现一个半透明幽灵，按你的真实轨迹移动、拾取、撤离
5. 屏幕角落显示实时对比：`你 ⌾3,200 vs 幽灵 ⌾2,800`
6. 结算屏给出三个维度的胜负

**为什么它能成立（三个设计支点）**：
- **潮汐确定性** → 同一时刻水位一致，公平性由机制保证，不需要服务器仲裁
- **零后端** → 分享码走 URL hash / 剪贴板，纯前端，静态托管可承载
- **异步** → 不需要同时在线，朋友可以在任何时间挑战你

### 4.3.1 前置缺口（已实现 · P1-c.0）：地图确定性

> 关键修正：幽灵竞速成立不能只靠「潮汐确定性」。潮汐曲线 `TideMission.PHASES` 是定值、全员一致（第 300 秒必为 0.83m），**但地图内容不是**——
> 战利品位置/类型/价值（`LootSystem.spawn`）与敌人初始位置（`Mission.start` → `randomLowPoint`）原本都走 `Math.random()`，
> 两个玩家即便同干员同武器，面对的战利品布局和敌人出生点也不同。没有它，「谁更快/更值」是一场假比赛。

**解法（已落地）**：引入可重放随机数源 `Rng` + `rrnd`，仅接管**生成期**的随机（`randomLowPoint`、`LootSystem.spawn`）；运行期敌人 AI 仍走 `Math.random`，手感不变。
`Mission.start(seed)` 每局记录一个 `Mission.seed`：正常游玩随机生成（与旧 `Math.random` 行为逐字节一致），挑战模式传入对方分享码里的种子 → 双方复现同一张图。
`test_seed_headless.js`（9 项）锁死：同 seed 逐字节一致 / 异 seed 区分 / 不传种子仍随机且被记录 / 运行期 AI 不被冻结。

> 这推翻了「先做录制和分享码编解码」的原顺序：编解码要携带的「种子」此前根本无法复现地图。正确顺序见 §4.5.1。

### 4.4 技术设计

**录制**（`GhostRecorder`，P1-c.1 已落地）：
- 采样 **每 2.0s 一次位置**（`interval:2.0`）。玩家 5.2m/s，2s 位移 ~10.4m，幽灵是参考线而非碰撞体，线性/Catmull-Rom 插值足够平滑
- 每点存：`t`（量化 ×10 → 0.1s）、`x,z`（量化 ×2 → 0.5m 网格）
- 事件流（含时间戳 + 位置）：`loot` 拾取 / `fire` 首次开火（破无痕）/ `kill` 击杀 / `extract` 撤离
- 编码：JSON → 定点量化 → `base64url`，再附 **FNV-1a 4 位校验和**。典型：10s 测试局 253 字节；长局（~4min / ~120 点）约 **1.2–2KB**
- `LastGhost` 在 `Mission.end` 时填充（seed/op/gun/novice/value/kills/t/samples），供 P1-c.2「生成潮痕」直接读取

> 难度一致性（已按用户拍板落地）：挑战模式 `Mission.start(seed, true)` 强制 `novice=false`，且**新手教学壳的全部效果都改挂 `this.novice` 而非 `isFirstRun`**——6 敌、水位×1.5、HARD_CAP 900、开局赠甲、撤离点全程高亮全部移除。两地的「公平复现」才有意义。

**回放**（`GhostPlayer`）：
- 按 `TideMission.t` 驱动（不是墙钟时间），保证与水位严格同步
- 渲染：半透明胶囊体 + 姓名标签 + 价值飘字
- 幽灵不参与碰撞、不受伤、不影响战利品（**幽灵拾取过的物品仍然存在**——否则变成互相使坏）

**防作弊的现实态度**：
本地录制必然可篡改。但我们不做排行榜，只做"朋友之间的挑战"，篡改的唯一受害者是自己和朋友。
→ **L0 不做信任签名**。但 P1-c.1 加了一道**数据完整性校验**（FNV-1a 4 位校验和）：只防传输/复制损坏（剪贴板截断、手滑改错一位），不防蓄意作弊。校验失败 → 解码返回 `null`，UI 直接提示「潮痕已损坏」。若未来做公开排行榜（L1/L2），再引入签名。

### 4.4.1 入口是情境化的，不是常驻的

**不要把「挑战」做成主菜单右上角的常驻入口。** 新玩家第一次进游戏时，这个入口对他毫无意义——
他还没打过一局，也没有朋友的潮痕。把无效入口放在最显眼位，是在消耗新手本就不多的注意力。

| 玩家状态 | 「挑战」入口 |
|---|---|
| 从未完成过一局 | 不显示 |
| 完成第一局后 | 结算屏首次出现「生成潮痕」按钮 |
| 之后 | 主菜单「挑战」与「开始撤离」**并列**（第二入口，非最显眼位） |

原则：**功能的曝光时机要匹配用户当下的能力和意图**。右上角最显眼位永远留给核心循环。

### 4.5 三个榜单（避免单一最优解）

只比"谁快"会让游戏退化成背板竞速。所以拆三个独立维度：

| 榜单 | 比什么 | 它鼓励的玩法 |
|---|---|---|
| **抢潮** | 撤离用时最短 | 路线规划、极限贪心 |
| **满载** | 带出价值最高 | 深入高危区、重量管理 |
| **无痕** | 0 击杀 + 撤离成功 | 潜行、绕行、听声辨位 |

「无痕」榜呼应掠波的解锁条件——**把单人教学变成了多人玩法**。这是设计上的自洽：我们教玩家"可以不用打"，然后给他一个专门奖励"不用打"的竞技场。

### 4.5.1 实施顺序（修正后）

原设想「先做录制和分享码编解码」被推翻——编解码要携带的「种子」在 P1-c.0 之前无法复现地图。正确顺序：

| 步骤 | 内容 | 状态 |
|---|---|---|
| **P1-c.0** | 可重放随机数源（注入种子 → 地图确定） | ✅ 已落地（9 项测试锁死） |
| **P1-c.1** | `GhostRecorder` 轨迹采样（2s + 事件流）+ 分享码编解码（seed/op/gun/novice/摘要 + FNV-1a 校验和，~2KB） | ✅ 已落地（34 项测试锁死） |
| **P1-c.2** | UI：首局结算「生成潮痕」/ 粘贴码加载幽灵 / 半透明幽灵渲染（最后做） | 待做 |

> 难度一致性提醒：当前难度随玩家进度浮动（首局 6 敌 / 之后 12 敌，line 2674）。幽灵竞速必须锁定同一难度档——分享码需编码 `novice` 标志，挑战双方强制跑同一档，否则比赛不公平。

### 4.6 递进路线

| 层级 | 内容 | 后端 | 何时做 |
|---|---|---|---|
| **L0** | 幽灵竞速（分享码） | 零 | **P1 现在就做**，约 1 周 |
| **L1** | 异步排行榜（全球潮痕榜） | 极简（Cloudflare KV / 静态 JSON） | P2，看 L0 使用率 |
| **L2** | 真·多人合作撤离 | 专用服务器 + 反作弊 | P3，**必须先有 EA 数据证明需求** |

**L2 的启动条件（写死，避免拍脑袋）**：EA 上线后，若 L0 幽灵分享率 > 30% 且社区出现"想同时在线"的明确诉求，才启动评估。

---

## 05. 分期与验收

| 阶段 | 内容 | 状态 | 验收标准 |
|:--:|---|:--:|---|
| **P1-a** | 成就式解锁（混合制）+ **三层提示** | ✅ **已完成** 2026-08-30 | 新玩家看完锁定卡，能说出"原来还能不开枪撤离" |
| **P1-b** | 地图配置化 | 待做 | 加一张新图只写一个配置对象，不碰逻辑代码 |
| **P1-c** | **幽灵竞速 L0**（含情境化入口） | **P1-c.0 + P1-c.1 已落地**（地图确定性 / 录制 / 分享码编解码） | P1-c.2 UI+幽灵渲染；两人用分享码跑同一张图，结算屏三榜对比，无后端 |
| **P1-d** | 专长系统（槽 A） | 待做 | 每个专长都改变一次可感知的决策 |
| **P2** | M02 白象街 + 泄洪机制 | 待做 | 老玩家在 M02 第一次泄洪时会"啊，水从上面来了" |

**P1-b 的启动时机**（不是现在）：配置化应该由**第二个实例**驱动。只有 M01 时做的抽象往往是错的抽象。
正确顺序是：**M02 地形设计定稿之后、编码之前**做配置化——那时才知道抽象该长什么样。

**P0 的那条验收标准不变**（设定集 §09）：
> 一个陌生人玩一局，结束后会说"刚才水涨上来的时候我慌了"。

**如果这句话没出现，后面所有内容都没有意义。**

---

*文档版本 v1.2 ｜ 2026-08-30 更新 ｜ 依据：01–07 号文档 + P0 收口 / P1-a / P1-c.0 / P1-c.1 落地实测数据*
*v1.2 变更：P1-c.1 已落地——`GhostRecorder`(2s 采样 + loot/fire/kill/extract 事件流) 与 `GhostCodec`(base64url + FNV-1a 校验和，~2KB) 全部 34 项测试通过；挑战模式 `novice` 强制 false 且教学壳全部改挂 `this.novice`。v1.1：P1-a 已落地；情境化入口 §4.4.1；P1-b 时机改为 M02 定稿后。*
