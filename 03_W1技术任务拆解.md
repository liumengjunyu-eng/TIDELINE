# 《TIDELINE 潮线》M0 · W1 技术任务拆解

> 引擎：**Unreal Engine 5（C++ 项目）** ｜ 周期：1 周 ｜ 人力：5 人
> W1 唯一目标：**水位系统跑起来，且能查到地图上任意一点的水深。**
> 其余一切都是次要的。

---

## 01. W1 的验收标准（先定终点）

这五条不满足，W1 不算完成，不允许进入 W2。**逐条可测，没有模糊空间。**

| # | 验收项 | 标准 | 测量方式 |
|:--:|---|---|---|
| **V1** | 水位连续变化 | 0.0 → 3.2m 平滑上涨，三阶段切换时刻误差 < 0.1s | 控制台打印时间戳 |
| **V2** | 水深查询精度 | 地图任意点查询水深，与实际几何误差 < **5cm** | 编辑器可视化调试 |
| **V3** | 查询性能 | 1000 次水深查询耗时 < **0.1ms** | Unreal Insights |
| **V4** | 移动状态机 | 四档状态正确切换，过渡无突变、无卡帧 | 手动试玩 + 录屏 |
| **V5** | 性能基线 | 空场景 1080p ≥ **300fps**（留足余量给 W2-W4） | Stat FPS |

**V5 为什么定 300 而不是 144**：W2 加机器人、W3 加 AI、W4 加埋点，性能只会往下掉。如果 W1 就只剩 144，W4 必然崩。**基线要往前留，不要往后借。**

---

## 02. 技术架构：水位系统

### 2.1 三个核心设计决策

决策一：**水位是全局标量，不是每区域配置**

```cpp
// 整个系统只有一个真值
float CurrentWaterLevel;  // 单位：米，全局唯一
```

所有区域的水深差异，由**地形标高**体现，不由水位配置体现。
这是整个系统最重要的简化——它让水位**可预测、可调试、可回放**。加一个断点就能知道全场水深。
反面教材：如果做成每区域独立涨水，第一版就会变成配置地狱，而且玩家永远算不准。

决策二：**用标高图查询，不用 LineTrace**

| 方案 | 单次耗时 | 问题 |
|---|:--:|---|
| LineTrace 向下 | 0.05–0.2ms | 20 人 × 每帧 = 不可接受；水面下穿透判定失效 |
| **标高图 Heightmap** | **~0.001ms（纹理采样）** | 需预烘焙；多层结构需额外处理 |

**标高图方案**：编辑期烘焙一张 `R32F` 纹理，存每个 XZ 位置的地面标高（米）。
运行时：`水深 = CurrentWaterLevel - Heightmap.Sample(WorldPos.XZ)`。O(1)，可在 GPU 批量算。

- 分辨率：1024 × 640 覆盖 60m × 40m → 每像素约 **5.9cm**，满足 V2 的 5cm 精度要求
- 存储：`Texture2D`，`R32F` 格式（32 位浮点，不要压缩，压缩会丢精度）
- 烘焙时机：编辑器内一键生成，关卡改动后重烘焙（纳入构建流程）

**多层结构的处理（已知的坑）**：标高图只能存一层高度。桥上/桥下会冲突。
**M0 的解法**：TIDE-01 只有一层地面 + 浮桥（浮桥作为动态对象单独处理，不走标高图）。
M1 若要做多层地图，再引入"分层标高图 + 层索引"。**现在不做，但架构要留口子**——标高图查询 API 预留 `LayerIndex` 参数，M0 恒为 0。

决策三：**状态机用四档，不是设定集里的两档**

设定集写的是 >1.2m 减速、>2.0m 游泳。W1 实现时细化为四档：

| 状态 | 水深 | 移动速度 | 能力限制 | 设计理由 |
|---|:--:|:--:|---|---|
| **Dry** | 0–0.3m | 100% | 无 | 脚踝以下不算水 |
| **Shallow** | 0.3–1.2m | **85%** | 冲刺距离 -30% | **新增档**。轻微惩罚 + 涉水声 + 波纹，给玩家预警 |
| **Wading** | 1.2–2.0m | **55%** | 不可冲刺、不可蹲 | 设定集原规则 |
| **Swimming** | > 2.0m | 40%（游泳） | 收起主武器，仅副武器/近战 | 设定集原规则 |

**为什么必须加 Shallow 档**：从 100% 速度直接掉到 55%，玩家的第一反应是"卡住了"而不是"我进水了"。
Shallow 档是一个**过渡带**——它让玩家在真正受限之前，先通过声音、波纹、轻微减速**感知到水**。
这直接对应风险登记表里的头号风险（"涉水状态手感差，玩家只觉得卡"）。**治病要在 W1，不要在 W5。**

**状态切换必须插值**：档位切换用 0.25s 缓动，不允许瞬变。

### 2.2 核心类设计

```
UTideSubsystem : UWorldSubsystem
├─ float CurrentWaterLevel            // 全局水位真值
├─ ETidePhase CurrentPhase            // Low / Rising / High
├─ float PhaseElapsed                 // 当前阶段已过时间
├─ FTidePhaseConfig PhaseConfig[3]    // 三阶段时长与目标水位
├─ UTerrainHeightmap* Heightmap       // 预烘焙标高图
├─ float QueryDepthAt(FVector)        // 查询任意点水深
├─ float QueryGroundHeightAt(FVector) // 查询地面标高
└─ FOnTidePhaseChanged OnPhaseChanged // 阶段切换事件（HUD / AI / 音效订阅）

UTideMovementComponent : UCharacterMovementComponent
├─ ETideWaterState CurrentState       // Dry / Shallow / Wading / Swimming
├─ float CurrentDepth                 // 每帧更新
├─ float StateBlendAlpha              // 状态过渡插值（0.25s）
├─ void UpdateTideState(float DeltaTime)
├─ void ApplyMovementModifier()
└─ FRippleEvent OnEnterWater          // 入水波纹事件（供音效/AI/埋点订阅）

UTideBuoyancyComponent : UActorComponent   // W2 实现，W1 仅占位
└─ 浮桥 / 集装箱随水位上浮

UTideHUDWidget : UUserWidget               // W2 实现，W1 仅打印到控制台
└─ 潮汐计时条
```

### 2.3 阶段切换时序（含预警期设计）

```
0s                    38s 40s              73s 75s              100s
├────── 低潮 Low ──────┤预├──── 涨潮 Rising ────┤警├── 满潮 High ──┤
     水位恒定 0.0m    警2s   线性 0.0 → 1.5m    2s   线性 1.5 → 3.2m
                      期                        期
```

**预警期（2 秒）是我要求加的，不在原设定集里。**

阶段切换前 2 秒触发：
- 水位**微涨 5cm**（视觉可察觉但不影响玩法）
- HUD 计时条变为橙色 + 闪烁
- 低频音效（水闸开启声）
- 潮位刻线高亮闪烁

**为什么必须加**：这直接决定 B1 指标（涨潮反应延迟，阈值 > 3.0s 判定失败）。
如果没有预警，玩家是"突然发现自己在水里"，反应延迟必然超标——但那不是机制不可读，是我们没给提示。
**B1 测的是机制可读性，不是玩家的反射速度。预警期把这两件事分开了。**

---

## 03. W1 任务清单

### 程序 A · 系统与工具（T1.1 – T1.4）

| ID | 任务 | 产出 | 工时 | 依赖 |
|:--:|---|---|:--:|---|
| **T1.1** | 搭建 UE5 C++ 项目骨架。禁用不必要插件（减少编译时间与包体） | 可编译项目 | 0.5d | — |
| **T1.2** | `UTideSubsystem`：水位真值、三阶段状态机、时间推进、事件广播 | 核心系统 | 1.5d | T1.1 |
| **T1.3** | **标高图烘焙工具**（Editor Utility Widget）：一键从关卡几何生成 R32F 标高图 | 编辑器工具 | 1.5d | T1.1 |
| **T1.4** | 水深查询 API：`QueryDepthAt` / `QueryGroundHeightAt`，含 `LayerIndex` 预留参数 | 查询接口 | 0.5d | T1.2, T1.3 |

### 程序 B · 角色与移动（T1.5 – T1.7）

| ID | 任务 | 产出 | 工时 | 依赖 |
|:--:|---|---|:--:|---|
| **T1.5** | 角色控制器基础：移动、冲刺、蹲、跳。**自己写，不要用 UE5 模板的移动代码** | 可控角色 | 1.5d | T1.1 |
| **T1.6** | `UTideMovementComponent`：四档状态机 + 速度修正 + 0.25s 插值 | 涉水系统 | 1.5d | T1.4, T1.5 |
| **T1.7** | 游泳态雏形：收枪、浮力上浮、视角切换 | 游泳原型 | 1d | T1.6 |

> **T1.5 的说明**：UE5 的 FPS 模板移动手感是"通用手感"，与我们要做的涉水状态机耦合会很痛苦（模板的 MovementMode 枚举与我们四档水位状态会打架）。
> 自己写一个精简的角色移动，前期多花半天，后面省一周。**这是 M0 阶段唯一允许的"重复造轮子"。**

### 关卡 · TIDE-01 灰盒（T1.8 – T1.10）

| ID | 任务 | 产出 | 工时 | 依赖 |
|:--:|---|---|:--:|---|
| **T1.8** | 按 02 文档 2.2 标高表搭建灰盒几何。**严格按标高，不许凭感觉** | 灰盒 v1 | 2d | — |
| **T1.9** | 潮位刻线占位：各区域墙面的橙色刻线（可用 Decal 或简单几何条） | 可读的潮位 | 1d | T1.8 |
| **T1.10** | 浮桥动态物体占位（静态网格 + 占位材质，W2 接浮力） | 浮桥占位 | 0.5d | T1.8 |

**T1.8 硬要求**：每个区域的实际标高必须与表格**逐米核对**，误差 > 10cm 返工。
这是整张图唯一的精度要求——标高错了，所有数据都是假的。

### 策划（T1.11 – T1.13）

| ID | 任务 | 产出 | 工时 |
|:--:|---|---|:--:|
| **T1.11** | 阶段切换时间参数表：三阶段时长、目标水位、预警期时长的初始值与可调范围 | 参数表 | 0.5d |
| **T1.12** | 埋点需求文档：M1–M6 + B1–B3 共 9 项指标的字段定义、触发时机、数据格式 | 埋点文档 | 1d |
| **T1.13** | 编辑器调试可视化：水深热力图开关（便于 T1.8 标高核对） | 需求文档 | 0.5d |

### TA（T1.14 – T1.16）

| ID | 任务 | 产出 | 工时 |
|:--:|---|---|:--:|
| **T1.14** | 水体临时材质：半透明 + 深度渐变 + 岸边泡沫线。**单色，不许做波纹动画** | 水体材质 | 1d |
| **T1.15** | 涉水视觉反馈原型：入水水花、涉水涟漪（简单粒子，够用即可） | 反馈原型 | 1d |
| **T1.16** | 性能基线测试与优化，确保 V5（空场景 ≥ 300fps） | 性能报告 | 1d |

---

## 04. W1 排期与依赖

```
         D1      D2      D3      D4      D5
程序A  ├ T1.1 ─┬──── T1.2 ────┬─ T1.4 ─┤
              └──── T1.3 ─────┘        │
程序B  ├──── T1.5 ────┬─── T1.6 ───┬─ T1.7 ─┤
                     └(依赖 T1.4)─┘
关卡   ├──────── T1.8 ────────┬─ T1.9 ─┬ T1.10 ─┤
策划   ├── T1.11 ─┬── T1.12 ──┤─ T1.13 ─┤
TA                ├── T1.14 ──┬─ T1.15 ─┬─ T1.16 ─┤
                                                  ▲
                                            D5 末：V1–V5 验收
```

**关键路径**：T1.1 → T1.2/T1.3 → T1.4 → T1.6 → T1.7
**最大风险点**：T1.3（标高图烘焙工具）无现成方案可抄，是自研工具。若 D3 末未跑通，立即降级为手工烘焙（见第 05 节）。

---

## 05. 风险与降级预案

| 风险 | 触发信号 | 降级方案 | 代价 |
|---|:--:|---|---|
| 标高图烘焙工具做不出来 | D3 末无法生成有效标高图 | **手工烘焙**：用蓝图在编辑期逐点 LineTrace 生成数据表（慢，但 W1 能用） | 烘焙从 1 秒变 5 分钟 |
| R32F 纹理在目标平台精度不足 | V2 误差 > 5cm | 改用 `R16F` + 标高偏移，或降分辨率至 512×320 并对边缘区域手工修正 | 精度降至约 12cm |
| 四档状态机过渡抖动 | 玩家在阈值边界反复触发切换 | 加**滞后区间（Hysteresis）**：进入用 1.2m，退出用 1.1m | 无实质代价，建议一开始就做 |
| 角色移动与状态机冲突 | 冲刺中入水导致状态错乱 | 状态切换时强制中断冲刺，播放 0.15s 减速动画 | 手感略顿，可接受 |
| 关卡进度落后（T1.8 未完成） | D4 末几何未搭完 | **砍掉浮桥占位（T1.10）**，先保证主区域标高正确 | 浮桥推迟到 W2 |

**滞后区间建议直接做进 T1.6**，不要等出问题再加——所有阈值状态机都需要它，这是常识性的，不是优化。

---

## 06. 明确不做的（W1）

- ❌ 机器人 AI（W3）
- ❌ 任何武器与射击（W3）——W1 的角色只是个能动的胶囊体
- ❌ 干员技能（W3）
- ❌ HUD（W2）——阶段切换先打印到控制台
- ❌ 网络同步（不阻塞 M0）
- ❌ 音效（W2，W1 的预警期用日志代替）
- ❌ 任何美术资产

---

## 07. 给 W2 的接口约定（现在就要定，否则 W2 会返工）

W1 结束时，以下接口必须稳定：

```cpp
// 查询类（供 W2 的 HUD / AI / 音效调用）
float GetCurrentWaterLevel() const;
ETidePhase GetCurrentPhase() const;
float GetPhaseRemainingTime() const;      // HUD 倒计时用
float QueryDepthAt(FVector WorldPos, int32 LayerIndex = 0) const;

// 事件类（观察者模式，订阅方不能反向修改水位）
DECLARE_MULTICAST_DELEGATE_TwoParams(FOnTidePhaseChanged, ETidePhase /*Old*/, ETidePhase /*New*/);
DECLARE_MULTICAST_DELEGATE_OneParam(FOnTideWarning, ETidePhase /*NextPhase*/);  // 预警期，提前 2s
```

**铁律：水位是只读的全局状态。任何系统都不得直接写入 `CurrentWaterLevel`。**
唯一的写入方是 `UTideSubsystem` 内部的 Tick。这条规矩现在不立，W3 加 AI 的时候一定会有人图省事直接改水位，然后系统就不可预测了。

---

*文档版本 v1.0 ｜ W1 启动*

**下一份文档**：`04_数值与平衡初稿.md`（3 把枪伤害/TTK、四档移动的精确参数、阶段时长参数表）
